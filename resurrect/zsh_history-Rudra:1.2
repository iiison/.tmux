nvim .tmux.conf.local
\ue0b0
echo -en "\ue0b0"
echo -en "\uE0B1"
echo -en "\uE0B2"
tmux a test
cp .tmux/.tmux.conf.local .
nvim ~/Documents/personal/jarvis2
tmux new -s test
eix
exl
\n
tmux kill-server
eixt
tmux a -t test
tmux new -s Rudra
nvim
../server
tmux new -s dataScience
cd ML
cd coursera-1/week-2/exercies/machine-learning-ex8
../ex8
octave-cli
cd ~/
git clone git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
tmux a -t Rudra
cd .tmux
open .
tmux source ~/.tmux.conf.local
tmux new -s demo
open ~/.tmux
tmux source ~/.tmux.conf
nvm ~/.tmux.conf.local
cd ~/Documents/personal/jarvis2
..
cd client
npm run build && rm -Rf ../server/artefacts/ && cp -R build/ ../server/artefacts
brew install terminal-notifier
brew install terminal-notifier && echo 'Completed' | terminal-notifier -sound default
npm run build && rm -Rf ../server/artefacts/ && cp -R build/ ../server/artefacts && echo 'Completed' | terminal-notifier -sound default
terminal-notifier -sound default
terminal-notifier -message Testing -sound default
terminal-notifier -message Testing
npm run build && rm -Rf ../server/artefacts/ && cp -R build/ ../server/artefacts && terminal-notifier -message Done
npm run build && rm -Rf ../server/artefacts/ && cp -R build/ ../server/artefacts; echo "Its done\n\n"
ri
sleep 2 ; echo ^G # Ctrl-v + Ctrl-g
ring
echo -e "\a"
sleep 2echo -e "\a"
sleep 2 echo -e "\a"
sleep 2 ; echo -e "\a"
npm install -g brb
npm run build && rm -Rf ../server/artefacts/ && cp -R build/ ../server/artefacts; brb
brb
npm install -g node-notifier
node-notifier
node
say error
npm run build && rm -Rf ../server/artefacts/ && cp -R build/ ../server/artefacts; say Done!
npm run build && rm -Rf ../server/artefacts/ && cp -R build/ ../server/artefacts; say "Deployemnt is completed"
npm run build && rm -Rf ../server/artefacts/ && cp -R build/ ../server/artefacts; say "Completed"
nvim ~/.tmux.conf.local
